<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kestir">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".uye_degistir"></activity>
        <activity android:name=".islem_degistir" />
        <activity android:name=".geldi" />
        <activity android:name=".admin_anasayfa" />
        <activity android:name=".bos" />
        <activity android:name=".sec" />
        <activity android:name=".randevular" />
        <activity android:name=".randevu_basarili" />
        <activity android:name=".sifre_yenile" />
        <activity android:name=".uye_anasayfa" />
        <activity android:name=".kayit_ol" />
        <activity android:name=".giris">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>